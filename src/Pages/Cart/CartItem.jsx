export default function CartItem ({id, img, title, price, company}) {
   // const {img, title, price, company } = product;
   

    return (
        <div className="cartItem">
            {/*
            THERE'S AN ISSUE HERE
            <img src={img} />
            */}
            
        </div>
    )
}